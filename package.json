{
  "name": "404-not-found",
  "version": "1.0.0",
  "description": "Page 404-not-found",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "postcss ./assets/postcss/style.css --output ./assets/css/style.css",
    "watch": "postcss ./assets/postcss/style.css --output ./assets/css/style.css --watch",
    "deploy": "gh-pages -d ."
  },
  "author": "Carlos Robertiz",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^5.1.12",
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0",
    "postcss-import": "^14.1.0"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
